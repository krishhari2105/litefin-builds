name: Moonfin Build & Release

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to build from Moonfin repository'
        required: true
        default: 'main'

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Generate Dynamic Metadata Tags
        id: prep
        run: |
          RAW_BRANCH="${{ github.event.inputs.branch }}"
          SANITIZED_BRANCH=$(echo "$RAW_BRANCH" | sed 's/[^a-zA-Z0-9._-]/_/g')
          echo "date_tag=$(date +'%Y.%m.%d')" >> $GITHUB_OUTPUT
          echo "branch_tag=${SANITIZED_BRANCH}" >> $GITHUB_OUTPUT

      - name: Checkout
        uses: actions/checkout@v4

      - name: Preserve profiles.xml
        run: cp profiles.xml /tmp/profiles.xml

      - name: Checkout Litefin repository
        uses: actions/checkout@v4
        with:
          repository: 'Moonfin-Client/Smart-TV'
          ref: ${{ github.event.inputs.branch }}

      - name: Restore profiles.xml
        run: cp /tmp/profiles.xml ${GITHUB_WORKSPACE}/profiles.xml    

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm

      - name: Install and Build Project
        run: |
          npm ci --force

      - name: Cache Tizen Studio
        id: cache-tizen
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/tizen-studio
          key: tizen-studio-10.0

      - name: Setup Tizen CLI
        if: steps.cache-tizen.outputs.cache-hit != 'true'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y expect curl
          curl -fL -o tizen-installer "https://download.tizen.org/sdk/Installer/tizen-sdk_10.0/web-cli_Tizen_SDK_10.0_ubuntu-64.bin"
          chmod +x tizen-installer
          ./tizen-installer --accept-license "${GITHUB_WORKSPACE}/tizen-studio"
          rm ./tizen-installer

      - name: Add Tizen Studio to PATH
        run: echo "${GITHUB_WORKSPACE}/tizen-studio/tools/ide/bin" >> $GITHUB_PATH

      - name: Decode Samsung Certificates
        env:
          AUTHOR_B64: ${{ secrets.SAMSUNG_AUTHOR_BASE64 }}
          DIST_B64: ${{ secrets.SAMSUNG_DISTRIBUTOR_BASE64 }}
        run: |
          echo "$AUTHOR_B64" | base64 -d > ${GITHUB_WORKSPACE}/author.p12
          echo "$DIST_B64"   | base64 -d > ${GITHUB_WORKSPACE}/distributor.p12

      - name: Copy Certificate Profile
        run: |
          mkdir -p ${GITHUB_WORKSPACE}/tizen-studio-data/profile
          cp ${GITHUB_WORKSPACE}/profiles.xml ${GITHUB_WORKSPACE}/tizen-studio-data/profile/profiles.xml

      # NEW: Dynamically inject the secret password into the runner's profile copy
      - name: Inject Certificate Password
        env:
          CERT_PASSWORD: ${{ secrets.SAMSUNG_CERT_PASSWORD }}
        run: |
          sed -i "s/SAMSUNG_PWD_PLACEHOLDER/${CERT_PASSWORD}/g" ${GITHUB_WORKSPACE}/tizen-studio-data/profile/profiles.xml

      - name: Compile and Repackage WGT
        run: |
          npm run build:webos
          npm run build:tizen 
          

      - name: Create or Update Litefin Release
        uses: ncipollo/release-action@v1
        with:
          allowUpdates: true
          tag: "moonfin-build-${{ steps.prep.outputs.branch_tag }}-${{ steps.prep.outputs.date_tag }}"
          name: "Moonfin Builds (${{ github.event.inputs.branch }}) - ${{ steps.prep.outputs.date_tag }}"
          body: "Manual build generated on ${{ steps.prep.outputs.date_tag }} from branch `${{ github.event.inputs.branch }}`."
          token: ${{ secrets.GITHUB_TOKEN }}
          artifacts: |
             *.wgt
             *.ipk
            
